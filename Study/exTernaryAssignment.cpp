#include <stdio.h>
/* Ternary Assignment
 */
int main() {	
	int age = 127;
	bool old = (age > 100) ? true : false;
	if(old) printf("isOld!"); else printf("not old.");
	return 0;
}







